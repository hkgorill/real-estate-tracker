# ─────────────────────────────────────────────
# Google 인증 (둘 중 하나만 필요)
# ─────────────────────────────────────────────

# [권장] Workload Identity Federation (GitHub Actions)
# → 별도 환경변수 불필요. workflow yml에서 google-github-actions/auth 사용
# → 설정 방법: docs/setup_workload_identity.md 참고

# [권장] 로컬 개발: gcloud CLI 로그인
# → 별도 환경변수 불필요
# gcloud auth application-default login --scopes=https://www.googleapis.com/auth/spreadsheets

# [레거시] Service Account 키 JSON 문자열 (키 생성 정책이 허용된 환경만)
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# [레거시] Service Account 키 파일 경로
# GOOGLE_SERVICE_ACCOUNT_JSON=./credentials/service_account.json

# ─────────────────────────────────────────────
# Google Sheets 설정
# ─────────────────────────────────────────────
GOOGLE_SHEETS_ID=your_spreadsheet_id_here

# ─────────────────────────────────────────────
# 수집 설정 (선택)
# ─────────────────────────────────────────────
REQUEST_DELAY_MIN=1.0      # 요청 간 최소 대기(초)
REQUEST_DELAY_MAX=3.0      # 요청 간 최대 대기(초)
REQUEST_TIMEOUT=30         # HTTP 요청 타임아웃(초)

# ─────────────────────────────────────────────
# 출력 설정 (선택)
# ─────────────────────────────────────────────
OUTPUT_CSV=false           # 로컬 CSV 출력 여부 (디버그용)
OUTPUT_CSV_PATH=./output
LOG_LEVEL=INFO
